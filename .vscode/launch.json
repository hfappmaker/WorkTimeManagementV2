{
    "configurations": [
        {
            "type": "node-terminal",
            "name": "スクリプトの実行: dev",
            "request": "launch",
            "command": "yarn run dev",
            "cwd": "${workspaceFolder}"
        }
    ]
}